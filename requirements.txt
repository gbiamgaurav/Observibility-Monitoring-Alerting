# HTTP framework + ASGI server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0

# Groq client (fast LLM inference)
groq>=0.9.0

# Prometheus metrics
prometheus-client==0.19.0

# OpenTelemetry — distributed tracing
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp-proto-grpc>=1.25.0
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-instrumentation-requests>=0.46b0

# Structured logging
structlog>=23.2.0
python-json-logger==2.0.7

# Kafka
kafka-python==2.0.2

# Data / ML  (numpy 2.x required for Python 3.13 wheels)
numpy>=2.0.0
scikit-learn>=1.5.0
pandas>=2.2.0

# Google Cloud — BigQuery log analytics + GCS storage
google-cloud-bigquery>=3.13.0
google-cloud-storage>=2.13.0

# Utilities
python-dotenv==1.0.0
pydantic>=2.8.0           # 2.8+ adds Python 3.13 support
pydantic-settings>=2.4.0
tenacity==8.2.3
aiohttp>=3.10.0           # 3.10+ fixes Python 3.13 compatibility
requests==2.31.0
